{
  "compilerOptions": {
    "target": "es5",
    "module": "commonjs",
    "outDir": "./", // Output directory set to the same directory as the source
    "rootDir": "./", // Root directory containing TypeScript files
    "strict": true
  },
  "include": [
    "src/**/*.ts" // Adjust the path based on your project structure
  ],
  "exclude": ["node_modules"]
}
